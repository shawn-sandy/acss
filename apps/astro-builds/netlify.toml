[build]
  base = "apps/astro-builds"
  publish = "dist"
  command = "npm install --prefix ../../packages/fpkit && npm run build"

[build.environment]
  NODE_VERSION = "22.12.0"
  NPM_FLAGS = "--production=false"
